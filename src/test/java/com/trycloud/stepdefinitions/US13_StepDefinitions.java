package com.trycloud.stepdefinitions;

import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;
import org.junit.Assert;

public class US13_StepDefinitions {
/*

    FilesPage filesPage = new FilesPage();


    @When("the user clicks the contacts module")
    public void theUserClicksTheContactsModule() {
        filesPage.contactsLink.click();
    }

    @Then("verify the contact {string} are in the list")
    public void verifyTheContactAreInTheList(String names) {

        int size = filesPage.allpeopleinContactList.size();
        System.out.println(size);
        Assert.assertTrue(size > 2);
        System.out.println("You have " + size + " users in your addressbook");
    }

}
*/
}
